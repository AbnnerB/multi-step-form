import styled from "styled-components";

const PrincipalContainer = styled.main`
  display: flex;
`;

function App() {
  return (
    <PrincipalContainer>
      <div>A pasta do projeto esta com as contas do vo</div>
    </PrincipalContainer>
  );
}

export default App;
